const { Router } = require('express');
const express = require('express');
const app = express();

//Get all the Product
// Write a code here with the help of Router to get all product
// Example:
// 1)Require Router
// 2)Define Router
router.get('/api/v1/products', (req, res) => {
  //Start Writing code from here
});

//Create a new Product with the help of router and add title into it
router.post('/api/v1/products', (req, res) => {
  //Start Writing code from here
});
module.exports = app;
